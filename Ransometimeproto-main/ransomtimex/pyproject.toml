[project]
name = "ransomtimex"
version = "1.0.0"
requires-python = "==3.12.*"
dependencies = [
  "fastapi>=0.111",
  "uvicorn[standard]>=0.30",
  "websockets>=12.0",
  "pydantic>=2.6",
  "PyYAML>=6.0",
  "numpy>=1.26",
  "scikit-learn>=1.4",
  "networkx>=3.2",
]

[tool.vercel]
entrypoint = "app:app"
